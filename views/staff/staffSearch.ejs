<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container my-3 py-3" style="font-family: 'Ubuntu', sans-serif;">
        <div class="row">
            <% for (let i = 0; i < 3; i++) { %>
            <div class="col-md-4">
                <div class="container my-3 py-3">
                    <form action="search_by_docno" method="POST">
                        <label class="text-primary fs-5" for="document_id_<%= i %>">Enter document no:</label><br>
                        <input type="text" id="document_id_<%= i %>" name="docNo" placeholder=" " required>
                        <button class="btn btn-sm btn-outline-primary" name="search">Search Documents</button>
                    </form>
                </div>
            </div>
            <% } %>
        </div>
    </div>
</body>
</html>